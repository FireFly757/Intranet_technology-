<UserControl x:Class="Lab2_PingApp.PingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab2_PingApp.PingControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500"
             d:DataContext="{d:DesignInstance local:PingControlVM}">
    <Grid Background="Lavender">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" MinHeight="27" />
            <RowDefinition Height="*" MinHeight="55" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0"  Text="{Binding Address}"/>
        <Label Grid.Row="1" Content="Здесь будут другие настройки" />
        <Button Grid.Row="2"
                Content="Запрос"
				Command="{Binding Path=SendRequestCommand}" Margin="200,2"/>
        <ListView ItemsSource="{Binding Path=Requests}" Grid.Row="3" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="300" Header="IP address" DisplayMemberBinding="{Binding Path=IpAddress}"/>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Path=RoadtripTime}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
